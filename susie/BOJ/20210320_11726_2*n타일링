#피보나치의 수 패턴을 가지고 있다. 패턴예측이 비교적 쉬운 문제, 다만 시간복잡도가 조금 더 높았던 이유는..?
#리스트를 li = [0] * 1001 하는것과 li.append('e')하는 것과의 시간 복잡도 차이 알아보기
import sys

n = int(sys.stdin.readline())
a,b = 0, 1
result = 0
li = []

for _ in range(n+2):
    li.append(result+a)
    a,b = b, a+b

print(li[n+1]%10007)
